const express = require('express');

const router = express.Router();

// Here I'm unsure how to access the User's Items using from the models.
//
const db = require('../models')

module.exports = function(app) {
    
}